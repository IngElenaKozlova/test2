// 1 check timtable.json recording['22:03:2023'][zal] на то что есть место для фильма ( если нет ошибка)
// 1 check timetable.json if next film doesn't start next day (we can't place the film after midnight)

// 2 sort array timtable.json time


// 3  create this check: we can't create setting.json until we have two keys: timeClear and startWork in !time in addTimetable in timetable.json

// 4 time no exist after calc time (00:00 and more) not place 
// 6 time - calc time between two films if has free time use it 